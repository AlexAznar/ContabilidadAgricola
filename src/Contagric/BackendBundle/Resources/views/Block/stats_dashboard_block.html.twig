{% extends sonata_block.templates.block_base %}

{% block block %}
  <div class="box">
                <div class="box-header">
                    <h3 class="box-title">{{ settings.title }}</h3>
                </div>
                <div class="box-body">
                    <table class="table table-hover">
                        <tbody>
                          <tr>
                            <td class="sonata-ba-list-label" width="40%">
                                Panel de consultas
                            </td>
                            <td>
                              <div class="btn-group">
                                <a class="btn btn-link btn-flat" href="{{ path('stats_pannel')}}>
                                   <i class="glyphicon glyphicon-stats"></i>  Graficos</a>
                              </div>
                            </td>
                          </tr>
                       </tbody>
                    </table>
                </div>
            </div>
{% endblock %}
